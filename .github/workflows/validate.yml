name: Validate OSINT Tools Watch

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check scripts and catalogue
        run: |
          python -m py_compile .watch/scripts/*.py
          python .watch/scripts/render_catalog.py --check
          python .watch/scripts/validate_catalog.py
